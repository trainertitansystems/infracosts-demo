name: Terraform PR Cost Delta Review

on:
  pull_request:
    branches: [ main ]
    paths:
      - "**/*.tf"

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  cost-diff:
    runs-on: ubuntu-latest

    steps:
      # -----------------------------------------------------
      # Setup tools
      # -----------------------------------------------------
      - uses: hashicorp/setup-terraform@v3

      - uses: infracost/actions/setup@v3
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/464753796348/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: github-terraform@siem-486017.iam.gserviceaccount.com
          create_credentials_file: true

      # -----------------------------------------------------
      # Checkout BASE (main)
      # -----------------------------------------------------
      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.ref }}
          path: base

      - name: Terraform Init (base)
        working-directory: ./base
        run: terraform init -reconfigure -input=false -lock=false

      - name: Terraform Plan (base)
        working-directory: ./base
        run: terraform plan -refresh=true -out=tfplan.binary -input=false -lock=false

      - name: Export base plan
        working-directory: ./base
        run: terraform show -json tfplan.binary > plan.json

      # -----------------------------------------------------
      # Checkout PR branch
      # -----------------------------------------------------
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr

      - name: Terraform Init (PR)
        working-directory: ./pr
        run: terraform init -reconfigure -input=false -lock=false

      - name: Terraform Plan (PR)
        working-directory: ./pr
        run: terraform plan -refresh=true -out=tfplan.binary -input=false -lock=false

      - name: Export PR plan
        working-directory: ./pr
        run: terraform show -json tfplan.binary > plan.json

      # -----------------------------------------------------
      # Infracost Diff (ONLY CHANGES)
      # -----------------------------------------------------
      - name: Run Infracost diff
        run: |
          infracost diff \
            --path ./pr/plan.json \
            --compare-to ./base/plan.json \
            --format json \
            --out-file infracost_diff.json

      - name: Generate diff table
        run: |
          infracost output \
            --path infracost_diff.json \
            --format table \
            > infracost.md

      - name: Extract Monthly Delta
        run: |
          DELTA=$(jq '.diffTotalMonthlyCost // 0' infracost_diff.json)
          echo "DELTA=$DELTA" >> $GITHUB_ENV
          echo "Monthly cost change: $DELTA"

      # -----------------------------------------------------
      # Build Gemini Request (Delta Only)
      # -----------------------------------------------------
      - name: Build Gemini request
        run: |
          jq -n \
            --arg delta "$DELTA" \
            --rawfile infra infracost_diff.json \
            '{
              contents: [{
                role: "user",
                parts: [{
                  text: (
                    "You are a FinOps Cloud Architect.\n\n" +
                    "Analyze ONLY the infrastructure changes in this PR.\n\n" +
                    "Monthly Cost Delta: $" + $delta + "\n\n" +
                    "INFRACOST DIFF DATA:\n" + $infra
                  )
                }]
              }]
            }' > gemini_request.json

      # -----------------------------------------------------
      # Call Gemini
      # -----------------------------------------------------
      - name: Call Gemini API
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=$GEMINI_API_KEY" \
            -H "Content-Type: application/json" \
            -d @gemini_request.json)

          BODY=$(echo "$RESPONSE" | head -n -1)
          STATUS=$(echo "$RESPONSE" | tail -n 1)

          if [ "$STATUS" != "200" ]; then
            echo "Gemini API failed" > gemini.md
            echo "$BODY"
          else
            echo "$BODY" | jq -r '.candidates[0].content.parts[0].text' > gemini.md
          fi

      # -----------------------------------------------------
      # Post PR Comment (Delta Only)
      # -----------------------------------------------------
      - name: Comment PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          {
            echo "## 💰 Terraform Cost Change (PR Only)"
            echo ""
            echo "**Monthly Cost Delta: $DELTA USD**"
            echo ""
            echo '```'
            cat infracost.md
            echo '```'
            echo ""
            echo "---"
            echo ""
            echo "## 🤖 Gemini AI Review (PR Changes Only)"
            echo ""
            cat gemini.md
          } > comment.md

          gh pr comment ${{ github.event.pull_request.number }} \
            --body-file comment.md
